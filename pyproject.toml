[project]
name = "mlops-final"
version = "0.1.0"
description = "MLOps demo with MLflow, Docker, and GitHub Actions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.10.0",
    "scikit-learn>=1.3.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0"
]
