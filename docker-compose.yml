version: '3.8'

services:
  mlflow-server:
    build: .
    ports:
      - "5000:5000"  # MLflow UI
      - "8000:8000"  # Prediction API
    volumes:
      - ./mlruns:/app/mlruns
    environment:
      - MLFLOW_TRACKING_URI=sqlite:////app/mlflow.db
